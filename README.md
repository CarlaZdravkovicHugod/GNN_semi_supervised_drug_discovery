# GNN_semi_supervised_drug_discovery
Semi-supervised learning for drug discovery using Graph Neural Networks for Deep Learning Course at DTU. 


Run the models by doing: python src/run.py model=gcn5

Submit a job to the HPC cluster by typing:

'''bsub < configs/bsubscripts/gcnX.sh'''

Replace X with the desired model number.